<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit <%= media.title %></title>
  <link rel="stylesheet" href="/css/Edit_style.css">
</head>

<body id="edit-page">

  <%- include("partials/header") %>

  <main>
    <section id="edit-section" class="edit-section">
      <h1>Edit Movie / TV Show</h1>

      <!-- Update Form -->
      <form id="edit-form" action="/mediadetails/<%= media._id %>/update" method="POST">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= media.title %>" required>

        <label for="synopsis">Synopsis:</label>
        <textarea id="synopsis" name="synopsis" rows="4" required><%= media.synopsis %></textarea>

        <label for="genre">Category / Genre:</label>
        <input type="text" id="genre" name="genre" value="<%= media.genre %>" required>

        <label for="rating">Rating (1–5):</label>
        <input type="number" id="rating" name="rating" min="1" max="5" value="<%= media.rating %>" step="0.1" required>

        <label for="small-poster">Small Poster URL:</label>
        <input type="text" id="small-poster" name="smallPoster" value="<%= media.smallPoster %>">

        <label for="large-poster">Large Poster URL:</label>
        <input type="text" id="large-poster" name="largePoster" value="<%= media.largePoster %>">

        <label for="price">Rental Price (per day):</label>
        <input type="number" id="price" name="priceperday" step="0.01" value="<%= media.priceperday %>" required>

        <label for="trailer">YouTube Trailer Link:</label>
        <input type="text" id="trailer" name="trailerLink" value="<%= media.trailerLink %>">

        <!-- Update Buttons (Conditional) -->
        <% if (media.isMovie) { %>
        <button type="submit" id="update-btn">Update Movie</button>
        <% } else { %>
        <button type="submit" id="update-btn">Update TV Show</button>
        <% } %>


      </form>

      <!-- Delete Option -->
      <div class="delete-container">
        <form action="/mediadetails/<%= media._id %>/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this item?');">
          <button id="delete-btn" type="submit">Delete Movie</button>
        </form>
      </div>

    </section>
  </main>

</body>
</html>
