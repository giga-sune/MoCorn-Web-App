<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> <%= media.title %> - Media Details</title>
  <link rel="stylesheet" href="/css/Detail_style.css">
</head>

<body id="details-page">

  <%- include("partials/header") %>

  <main>
    <section id="media-details" class="details-section">
      <h1 id="media-title"><%= media.title %></h1>

      <div class="poster-container">
        <img id="small-poster" src="<%= media.smallPoster %>" alt="<%= media.title %> small poster">
        <img id="large-poster" src="<%= media.largePoster %>" alt="<%= media.title %> large poster">
      </div>

      <div class="media-info">
        <p id="media-synopsis"><strong>Synopsis:</strong> <%= media.synopsis %></p>
        <p id="media-genre"><strong>Genre:</strong> <%= media.genre %></p>
        <p id="media-rating"><strong>Rating:</strong> <%= media.rating %> / 5 ★★★★☆</p>
        <p id="media-price"><strong>Rental Price:</strong> $<%= media.priceperday %> per day</p>
        <p id="media-trailer">
          <strong>Trailer:</strong>
          <a href="<%= media.trailerLink %>" target="_blank">Watch on YouTube</a>
        </p>
      </div>

      <div class="details-actions">
        <form action="/mediadetails/<%= media._id %>/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this?');">
          <button id="delete-btn" type="submit">Delete</button>
        </form>

        <form action="/mediadetails/<%= media._id %>/edit" method="GET">
          <button id="edit-btn" type="submit">Edit</button>
        </form>
      </div>
    </section>
  </main>

</body>
</html>
